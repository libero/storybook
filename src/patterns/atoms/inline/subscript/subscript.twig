{%- import '../../../html.twig' as html -%}

{%- set attributes = attributes|default({}) -%}

{% spaceless %}

  <sub {{- html.attributes(attributes|default({})) }}>

    {{- html.content(text) -}}

  </sub>

{% endspaceless %}
